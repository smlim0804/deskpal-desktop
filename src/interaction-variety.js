export const SOCIAL_VARIETY = Object.freeze({
  en: Object.freeze({
    openers: Object.freeze([
      "slides beside",
      "loops around",
      "matches pace with",
      "pauses near",
      "cuts a tiny circle by",
      "drifts close to",
      "bounces next to",
      "keeps watch beside",
    ]),
    tempo: Object.freeze(["softly", "briskly", "carefully", "playfully", "sleepily", "brightly"]),
    props: Object.freeze([
      "warm pixel",
      "tiny sparkle",
      "trail crumb",
      "screen corner",
      "little signal",
      "soft shadow",
      "cursor echo",
      "desktop breeze",
    ]),
    outcomes: Object.freeze([
      "becomes a team ritual",
      "turns into a better route",
      "raises the room mood",
      "stores a useful memory",
      "makes the next move smoother",
      "builds a little trust",
      "becomes a shared trick",
      "leaves a bright afterglow",
    ]),
  }),
  ko: Object.freeze({
    openers: Object.freeze([
      "옆으로 살짝 붙어서",
      "작은 원을 그리며",
      "속도를 맞춰서",
      "근처에서 잠깐 멈춰",
      "가볍게 돌며",
      "조용히 다가가",
      "통통 튀듯 옆에 서서",
      "곁을 지키며",
    ]),
    tempo: Object.freeze(["부드럽게", "재빠르게", "조심스럽게", "장난스럽게", "졸린 듯", "반짝이며"]),
    props: Object.freeze([
      "따뜻한 픽셀",
      "작은 반짝임",
      "꼬리 잔상",
      "화면 모서리",
      "작은 신호",
      "포근한 그림자",
      "커서 메아리",
      "데스크톱 바람",
    ]),
    outcomes: Object.freeze([
      "팀 루틴이 됐어",
      "더 좋은 경로가 됐어",
      "방 분위기를 올렸어",
      "쓸만한 기억으로 저장됐어",
      "다음 움직임을 더 매끄럽게 했어",
      "작은 신뢰를 쌓았어",
      "같이 쓰는 기술이 됐어",
      "밝은 여운을 남겼어",
    ]),
  }),
});

export const DISCOVERY_VARIETY = Object.freeze({
  en: Object.freeze({
    places: Object.freeze([
      "near the window edge",
      "between app shadows",
      "beside a desktop corner",
      "where the cursor passed",
      "under a bright pixel",
      "along the lower screen path",
      "behind a quiet icon",
      "inside a tiny empty gap",
    ]),
    finds: Object.freeze([
      "spark seed",
      "mini shell",
      "dust star",
      "smiling sticker",
      "lost button",
      "tiny ribbon",
      "glow pebble",
      "cloud chip",
      "yellow pixel",
      "small map",
    ]),
    actions: Object.freeze([
      "nudged it forward",
      "checked it with a spin",
      "marked it with a tail flick",
      "picked it up with a hop",
      "carried it carefully",
      "showed it off in a loop",
      "left a tiny footprint",
      "shook it once for luck",
    ]),
    outcomes: Object.freeze([
      "raised the mood",
      "stored a new memory",
      "found a small reward",
      "mapped the next walk",
      "expanded the patrol route",
      "practiced motion timing",
      "left a friendly signal",
      "collected a bit of luck",
    ]),
  }),
  ko: Object.freeze({
    places: Object.freeze([
      "창가 모서리 근처",
      "앱 그림자 사이",
      "데스크톱 구석 옆",
      "커서가 지나간 자리",
      "밝은 픽셀 아래",
      "화면 아래 산책길",
      "조용한 아이콘 뒤",
      "작은 빈 공간 안",
    ]),
    finds: Object.freeze([
      "반짝 씨앗",
      "미니 조개",
      "먼지 별",
      "웃는 스티커",
      "잃어버린 버튼",
      "작은 리본",
      "빛나는 돌멩이",
      "구름 조각",
      "노란 픽셀",
      "작은 지도",
    ]),
    actions: Object.freeze([
      "코로 톡 밀어보고",
      "한 바퀴 돌며 확인하고",
      "꼬리로 표시하고",
      "짧게 점프해서 집고",
      "조심히 물고 와서",
      "빙글 돌며 자랑하고",
      "작은 발자국을 남기고",
      "운 좋으라고 한 번 흔들고",
    ]),
    outcomes: Object.freeze([
      "기분을 올렸어",
      "새 기억으로 저장했어",
      "작은 보상을 찾았어",
      "다음 산책길을 그렸어",
      "순찰 경로를 넓혔어",
      "움직임 타이밍을 익혔어",
      "친근한 신호를 남겼어",
      "작은 운을 모았어",
    ]),
  }),
});

export const PATROL_VARIETY = Object.freeze({
  en: Object.freeze({
    starts: Object.freeze([
      "checked the lower edge",
      "mapped a quiet app shadow",
      "followed a cursor echo",
      "circled a bright desktop gap",
      "watched the window border",
      "tested a tiny patrol lane",
      "sniffed out a hidden route",
      "paced beside a soft icon glow",
    ]),
    obstacles: Object.freeze([
      "a sleepy pixel bump",
      "a folded trail mark",
      "a blinking corner signal",
      "a dust-sized shortcut",
      "a wobbly screen breeze",
      "a shy sparkle trail",
      "a tiny timing challenge",
      "a quiet gap between windows",
    ]),
    reactions: Object.freeze([
      "kept its rhythm steady",
      "made a quick little turn",
      "left a clean route marker",
      "waited, then moved with care",
      "saved the safest path",
      "practiced a sharper step",
      "shared the route with friends",
      "turned it into a tiny habit",
    ]),
    results: Object.freeze([
      "the next roam feels smoother",
      "the desktop feels friendlier",
      "a small reward trail appeared",
      "the patrol map grew a little",
      "its confidence ticked upward",
      "the route became easier to repeat",
      "the room mood lifted softly",
      "a useful memory was stored",
    ]),
  }),
  ko: Object.freeze({
    starts: Object.freeze([
      "화면 아래 가장자리를 살피고",
      "조용한 앱 그림자를 기록하고",
      "커서 메아리를 따라가고",
      "밝은 데스크톱 틈을 한 바퀴 돌고",
      "창 테두리를 지켜보고",
      "작은 순찰길을 시험하고",
      "숨은 경로를 찾아보고",
      "부드러운 아이콘 빛 옆을 걸으며",
    ]),
    obstacles: Object.freeze([
      "졸린 픽셀 턱",
      "접힌 잔상 표시",
      "깜빡이는 모서리 신호",
      "먼지만 한 지름길",
      "흔들리는 화면 바람",
      "수줍은 반짝 길",
      "작은 타이밍 과제",
      "창 사이의 조용한 틈",
    ]),
    reactions: Object.freeze([
      "리듬을 차분히 유지했어",
      "재빠르게 방향을 바꿨어",
      "깨끗한 경로 표시를 남겼어",
      "잠깐 기다린 뒤 조심히 움직였어",
      "가장 안전한 길을 저장했어",
      "더 날카로운 발걸음을 연습했어",
      "친구들과 경로를 공유했어",
      "작은 습관으로 만들었어",
    ]),
    results: Object.freeze([
      "다음 이동이 더 부드러워졌어",
      "데스크톱이 조금 더 친근해졌어",
      "작은 보상 길이 열렸어",
      "순찰 지도가 조금 넓어졌어",
      "자신감이 살짝 올랐어",
      "다시 돌기 쉬운 길이 됐어",
      "방 분위기가 부드럽게 올랐어",
      "쓸만한 기억으로 저장됐어",
    ]),
  }),
});

export const MICRO_EVENT_VARIETY = Object.freeze({
  en: Object.freeze({
    moments: Object.freeze([
      "finds a tiny rhythm",
      "tests a small habit",
      "notices a soft signal",
      "turns a pause into practice",
      "makes a little choice",
      "checks the room mood",
      "tries a quiet trick",
      "follows a bright cue",
    ]),
    motions: Object.freeze([
      "with a careful wiggle",
      "with a proud little loop",
      "while keeping its pace steady",
      "with a blink of pixel light",
      "without leaving your side",
      "with a quick tail trace",
      "like it learned something",
      "as if the desktop answered",
    ]),
    contexts: Object.freeze([
      "near the room edge",
      "beside the shortcut trail",
      "under the current window light",
      "where the last sparkle faded",
      "around the soft cursor path",
      "inside a calm screen gap",
      "between two tiny memories",
      "next to a familiar route",
    ]),
    results: Object.freeze([
      "and the next care step feels clearer",
      "and a small memory settles in",
      "and its mood gets easier to read",
      "and the room feels more alive",
      "and its movement becomes smoother",
      "and the bond grows a little warmer",
      "and the day gains a tiny goal",
      "and the companion looks more confident",
    ]),
  }),
  ko: Object.freeze({
    moments: Object.freeze([
      "작은 리듬을 찾고",
      "짧은 습관을 시험하고",
      "부드러운 신호를 눈치채고",
      "잠깐의 멈춤을 연습으로 바꾸고",
      "작은 선택을 하고",
      "방 분위기를 확인하고",
      "조용한 기술을 시도하고",
      "밝은 단서를 따라가고",
    ]),
    motions: Object.freeze([
      "조심스러운 꼼지락으로",
      "뿌듯한 작은 루프로",
      "속도를 차분히 유지하며",
      "픽셀빛을 깜빡이며",
      "네 곁을 벗어나지 않고",
      "빠른 꼬리 자국으로",
      "뭔가 배운 것처럼",
      "데스크톱이 대답한 것처럼",
    ]),
    contexts: Object.freeze([
      "방 가장자리 근처에서",
      "바로가기 흔적 옆에서",
      "현재 창빛 아래에서",
      "마지막 반짝임이 사라진 곳에서",
      "부드러운 커서 길 주변에서",
      "차분한 화면 틈 안에서",
      "작은 기억 두 개 사이에서",
      "익숙한 경로 옆에서",
    ]),
    results: Object.freeze([
      "다음 돌봄이 더 또렷해졌어",
      "작은 기억으로 자리 잡았어",
      "기분을 읽기 쉬워졌어",
      "방이 조금 더 살아났어",
      "움직임이 더 부드러워졌어",
      "친밀도가 조금 더 따뜻해졌어",
      "하루에 작은 목표가 생겼어",
      "친구가 더 자신 있어 보여",
    ]),
  }),
});

const MOOD_VARIANTS = 6;
const STAGE_VARIANTS = 3;
const TOY_VARIANTS = 7;
const SOCIAL_TYPES = 5;
const PATROL_ROUTE_TYPES = 4;
const MICRO_EVENT_TYPES = 6;

export function hashText(value) {
  const text = String(value || "");
  let hash = 2166136261;
  for (let index = 0; index < text.length; index += 1) {
    hash ^= text.charCodeAt(index);
    hash = Math.imul(hash, 16777619);
  }
  return hash >>> 0;
}

export function pickVariant(items, seed, offset = 0) {
  if (!Array.isArray(items) && !ArrayBuffer.isView(items)) return "";
  if (!items.length) return "";
  return items[(hashText(`${seed}:${offset}`) + offset * 97) % items.length];
}

function languagePack(source, language) {
  return source[language] || source.en;
}

function countPack(pack) {
  return Object.values(pack).reduce((total, items) => total * (Array.isArray(items) ? items.length : 1), 1);
}

export function socialVarietyCount() {
  return SOCIAL_TYPES * countPack(SOCIAL_VARIETY.en) * MOOD_VARIANTS * STAGE_VARIANTS * TOY_VARIANTS;
}

export function discoveryVarietyCount() {
  return countPack(DISCOVERY_VARIETY.en) * MOOD_VARIANTS * STAGE_VARIANTS * TOY_VARIANTS;
}

export function patrolVarietyCount() {
  return PATROL_ROUTE_TYPES * countPack(PATROL_VARIETY.en) * MOOD_VARIANTS * STAGE_VARIANTS * TOY_VARIANTS;
}

export function microEventVarietyCount() {
  return MICRO_EVENT_TYPES * countPack(MICRO_EVENT_VARIETY.en) * MOOD_VARIANTS * STAGE_VARIANTS * TOY_VARIANTS;
}

export function buildSocialDetail(options) {
  const language = options?.language === "ko" ? "ko" : "en";
  const pack = languagePack(SOCIAL_VARIETY, language);
  const seed = options?.seed || `${options?.actorName}:${options?.otherName}:${options?.type}`;
  const opener = pickVariant(pack.openers, seed, 1);
  const tempo = pickVariant(pack.tempo, seed, 2);
  const prop = options?.toyName || pickVariant(pack.props, seed, 3);
  const outcome = pickVariant(pack.outcomes, seed, 4);
  const phrase = options?.phrase || options?.type || "interacted";
  const mood = options?.mood || "";
  const stage = options?.stage || "";

  if (language === "ko") {
    return `${opener} ${tempo} ${phrase}. ${stage} ${mood} 상태라 ${prop} 신호가 ${outcome}`;
  }
  return `${opener} ${tempo} ${phrase}. With ${stage} ${mood} energy, the ${prop} cue ${outcome}`;
}

export function buildDiscoveryDetail(options) {
  const language = options?.language === "ko" ? "ko" : "en";
  const pack = languagePack(DISCOVERY_VARIETY, language);
  const seed = options?.seed || `${options?.characterName}:${options?.reward}`;
  const place = pickVariant(pack.places, seed, 1);
  const find = options?.findName || pickVariant(pack.finds, seed, 2);
  const action = pickVariant(pack.actions, seed, 3);
  const outcome = pickVariant(pack.outcomes, seed, 4);
  const characterName = options?.characterName || "Companion";
  const reward = Math.max(1, Math.round(Number(options?.reward) || 1));
  const coinText = options?.coinText || "coins";

  if (language === "ko") {
    return `${characterName}가 ${place}에서 ${find}을 발견했어. ${action} ${outcome}. +${reward} ${coinText}`;
  }
  return `${characterName} found a ${find} ${place}. It ${action} and ${outcome}. +${reward} ${coinText}`;
}

export function buildPatrolDetail(options) {
  const language = options?.language === "ko" ? "ko" : "en";
  const pack = languagePack(PATROL_VARIETY, language);
  const seed = options?.seed || `${options?.characterName}:${options?.routeName}:${options?.rewardText}`;
  const start = pickVariant(pack.starts, seed, 1);
  const obstacle = pickVariant(pack.obstacles, seed, 2);
  const reaction = pickVariant(pack.reactions, seed, 3);
  const result = pickVariant(pack.results, seed, 4);
  const characterName = options?.characterName || "Companion";
  const routeName = options?.routeName || "";
  const mood = options?.mood || "";
  const stage = options?.stage || "";
  const toyName = options?.toyName || "";
  const rewardText = options?.rewardText || "";

  if (language === "ko") {
    const toy = toyName ? ` ${toyName} 감각으로` : "";
    return `${characterName}가 ${routeName} 순찰에서 ${start} ${obstacle}을 만났어.${toy} ${stage} ${mood} 상태로 ${reaction}. ${result}. ${rewardText}`;
  }
  const toy = toyName ? ` With ${toyName} timing,` : "";
  return `${characterName} ${start} on ${routeName} patrol and met ${obstacle}.${toy} in ${stage} ${mood} mood it ${reaction}. ${result}. ${rewardText}`;
}

export function buildMicroEventDetail(options) {
  const language = options?.language === "ko" ? "ko" : "en";
  const pack = languagePack(MICRO_EVENT_VARIETY, language);
  const seed = options?.seed || `${options?.characterName}:${options?.eventName}:${options?.mood}`;
  const moment = pickVariant(pack.moments, seed, 1);
  const motion = pickVariant(pack.motions, seed, 2);
  const context = options?.themeName || pickVariant(pack.contexts, seed, 3);
  const result = pickVariant(pack.results, seed, 4);
  const characterName = options?.characterName || "Companion";
  const eventName = options?.eventName || "Tiny Moment";
  const mood = options?.mood || "";
  const stage = options?.stage || "";
  const toyName = options?.toyName || "";
  const personality = options?.personality || "";
  const rewardText = options?.rewardText || "";

  if (language === "ko") {
    const toy = toyName ? ` ${toyName} 감각을 섞어` : "";
    return `${characterName}가 ${eventName}에서 ${context} ${moment} ${motion}${toy}. ${personality} ${stage} ${mood} 상태라 ${result}. ${rewardText}`;
  }
  const toy = toyName ? ` with ${toyName} timing` : "";
  return `${characterName} ${moment} during ${eventName} ${context} ${motion}${toy}. In ${personality} ${stage} ${mood} mode, ${result}. ${rewardText}`;
}
